#include "main.h"
/**
 * _isupper - Checks uppercase
 * @x: Number to be checked
 * Return: 1 for upper, 0 for rest
 */
int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
